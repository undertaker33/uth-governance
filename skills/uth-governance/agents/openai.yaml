interface:
  display_name: "UTH Governance"
  short_description: "Top-level UTH scene router"
  default_prompt: "Use uth-governance in UTH-enabled projects with .uth-governance/project.json to classify requests, route to the right uth-* scene, stop on unclear scenes, and keep non-onboarded projects silent unless the user asks for uth-onboarding."
